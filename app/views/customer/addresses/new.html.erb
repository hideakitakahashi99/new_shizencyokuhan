
<div class="container">
	<div class="row">
		<div class="span8">
<div class="depot_form">
	<%= form_for [:customer, :staff_member, @address] do |f| %>
		<%= render 'form', f: f %>
		
		
<div class="actions">
	<%= f.submit '新規登録' %>
</div>
	
</fieldset>

<% end %>
</div>
</div>
</div>
</div>
