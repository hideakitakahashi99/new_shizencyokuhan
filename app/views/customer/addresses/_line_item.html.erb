	<% if line_item == @current_item %>
	<tr id="current_item">
	<% else %>
	<tr>
	<% end %>
		<td>【<%= line_item.product.staff_member.farm_name %>】<%= line_item.product.category %>|<%= line_item.product.variety %> </td>
		<td> &times;<%= line_item.quantity %></td>
		<td class="item_price"><%= sprintf("¥%0.0f", line_item.total_price) %></td>
	</tr>
