<div class="container">
  <div class="row">
    <div class="span12">
      <% @title = '加工品 情報編集' %>
      <h1><%= @title %></h1>

      <div id="generic-form">
      <div class="pure-g">
            <div class="l-box-lrg pure-u-1 pure-u-md-1">
      <%= form_for [ :staff, @product ], html: {multipart: true} do |f| %>
        <%= render 'pg_form', f: f %>
        
        <% end %>
      </div></div>
      </div>
    </div>
  </div>
</div>
